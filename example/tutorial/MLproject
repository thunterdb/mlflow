name: tutorial

conda_env: conda.yaml

entry_points:
  scikit:
    parameters:
      alpha: float
      l1_ratio: {type: float, default: 0.1}
    command: "python train.py {alpha} {l1_ratio}"

  tf:
    command: "python tensorflow_example.py"
